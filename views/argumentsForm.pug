extends ./base
include ./elements/buttonIcon
include ./elements/argumentsFormWidget

block content 
    form(action=nextStep method="post") 
        header 
            h3 Schritt #{currentStep}:
            h1 #{debate.thesis}
            h3 #{title}
            input(type="text" hidden value=debate.thesis name="thesis")
        main 
            +argumentsFormWidget(debate)
        footer 
            button(type="submit") Weiter
                +fancyIcon("⇒")
    script(src="/assets/js/formManager.js") 
